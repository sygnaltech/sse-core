/**
 * Standard FIX Triggers
 * Import all standard triggers to register them via decorators
 */

// Import to trigger @trigger decorators
import './trigger-click';

// Export classes for type usage
export { TriggerClick } from './trigger-click';
